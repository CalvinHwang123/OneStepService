<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" 
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="oss.mapper.BusiManageMapper">
	
	<select id="demandList" resultType="oss.bean.demands">
		select * from demands where 1 = 1
	</select>
	<update id="examine" parameterType="long">
	update  demands set demandstatusid = 2 where demandID=#{demandID}
	</update>
	
	<update id="Audited" parameterType="long">
	update  demands set demandstatusid = 3 where demandID=#{demandID}
	</update>
	<!-- <update id="providerupdatepow" parameterType="long">
	update  Users set userPwd =123456 where userID=#{userID}
	</update>
	
	<update id="providerBlacklist" parameterType="long">
	update  Users set userStatusID =3 where userID=#{userID}
	</update>
	
	<update id="providerDisable" parameterType="long">
	update  Users set userStatusID =2 where userID=#{userID}
	</update>
	
	<select id="providerlist" resultType="oss.bean.Users">
		select * from Users where 1 = 1
	</select>
	
	<select id="searchUser" resultType="oss.bean.Users">
		select * from Users where userAccount = #{userAccount}
	</select> -->
	
</mapper>
